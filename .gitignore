# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
.vscode/
.idea/

# Local dev
node_modules/
.cache/

# Local Claude settings (kept per-machine)
.claude/settings.local.json

# Never commit these — runtime artifacts or secrets
*.pdf
# Built-in worksheet PDFs are bundled with the app.
!public/worksheets/*.pdf
*.env
.env.*
secrets.json
